//! Chrome-piece painters: the header strips, the corner box, and the
//! frozen-row/column separators. Each file adds an `impl RendererCore` block
//! for one piece.

pub mod corner_box;
pub mod frozen_separator;
pub mod header_strip;
