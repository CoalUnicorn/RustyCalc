[package]
name = "iron-canvas-recorder"
version.workspace = true
authors.workspace = true
edition.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
iron-canvas-core = { path = "../iron-canvas-core" }
